{# templates/index.html.twig #}
{% extends 'admin/layout.html.twig' %}

{% block contenu %}
<!--MENU ETAPES -->

<h2>Initialisation</h2>

<br />

<p class="alert alert-info">
	<strong>Effacer la base</strong><br /><br />
	Cette action va effacer les éléments suivant de la base de donnée : <br />
	<span class="badge badge-secondary">Eleves <span class="badge badge-danger">{{ eleves }}</span></span></button>
	<span class="badge badge-secondary">Groupes <span class="badge badge-danger">{{ groupes }}</span></span><br /><br />
	Les éléments suivants seront conservés, mais vous pourrez les modifier : <br />
	<span class="badge badge-secondary">Ateliers <span class="badge badge-info">{{ ateliers }}</span></span>
	<span class="badge badge-secondary">Intervenants<span class="badge badge-info">{{ intervenants }}</span> </span>
	<span class="badge badge-secondary">Salles <span class="badge badge-info">{{ salles }}</span></span>
	<span class="badge badge-secondary">Horaires </span><br />
	<br /><br />
	<button class="btn btn-danger" data-href="{{ path('app_admin_reinitialisation') }}" data-toggle="modal" data-target="#confirm-delete">
        Réinitialisation
    </button>
</p>

	

	<div class="modal fade" id="confirm-delete" tabindex="-1" role="dialog">
	  <div class="modal-dialog" role="document">
	    <div class="modal-content">
	      <div class="modal-header">
	        <h5 class="modal-title">Demande de confirmation</h5>
	        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
	          <span aria-hidden="true">&times;</span>
	        </button>
	      </div>
	      <div class="modal-body">
	        <p>Etes-vous sur de vouloir procéder à la réinitialisation de la base ? (action irréversible)</p>
	      </div>
	      <div class="modal-footer">
	      	<button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
            <a class="btn btn-danger btn-ok">Réinitialisaer</a>
	      </div>
	    </div>
	  </div>
	</div>





{% endblock %}


{% block script %}
<script>
        $('#confirm-delete').on('show.bs.modal', function(e) {
            $(this).find('.btn-ok').attr('href', $(e.relatedTarget).data('href'));
            
            $('.debug-url').html('Delete URL: <strong>' + $(this).find('.btn-ok').attr('href') + '</strong>');
        });
    </script>
{% endblock %}